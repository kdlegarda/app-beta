app_beta=# CREATE TABLE users(
app_beta(# nombre varchar(100),
app_beta(# apellido varchar(100),
app_beta(# telefono varchar(100),
app_beta(# email varchar(100),
app_beta(# contraseña varchar(100),
app_beta(# id varchar(100)
app_beta(# );
CREATE TABLE
app_beta=#git 